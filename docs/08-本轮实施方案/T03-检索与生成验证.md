# T03 资料检索与生成验证 — 实现计划与交接记录

> 任务定义见[任务池](../TEAM_DEVELOPMENT_TASKS.md)，字段口径以[接口与 Mock 约定](02-接口与Mock约定.md)为准。
> 分支：`t03-retrieval-source-verification`。本文件同时充当实现计划与交接证据，未完成部分在文末单列。

## 1. 领取卡片

| 项 | 内容 |
|---|---|
| 任务 | T03 资料检索与生成验证 — 固定允许范围、来源定位、一次真实模型输出及失败记录 |
| 负责人 | 待填 |
| 实际消费者 | T09（项目资料与来源）；T10 生成任务复用同一 `Source` 形状 |
| 评审人 | **空缺** — T09 目前无人领取，见 §6.1 |
| 用户结果 | 能在真实资料上跑一次检索与生成，每条结论能定位回原文；范围被限制在显式允许集合内，任何被拒/未就绪/降级都如实出现，不冒充成功 |
| 输入/输出 | 输入：契约 `Asset`/`Source` 形状（`contracts/openapi.json`）；输出：`AssetGateway` / `SourcePolicy` Go 小接口 + 固定实现 + 验证报告 |
| 界面切片 | 不适用（T03 不在 `02-接口与Mock约定.md §4` 的界面切片表内） |
| 可先开发 | 合成资料 + 固定允许集合；仓库已有检索链路（`KnowledgeBaseService.HybridSearch`） |
| 接真前置 | T01（运行方式）— **本任务刻意不依赖它**，见 §2 |
| 必验场景 | 成功：允许集合内检索并定位回原文；关键失败：空范围、部分授权、未就绪、定位失效、模型不可用 |
| 交接证据 | §7 |
| 剩余工作量 | 约 10–16 小时（含测试与记录）；未测风险见 §6 |
| 当前阻塞 | 无。真实模型回路未跑通属**已记录的 `not_run`**，不阻塞其余断言 |
| 状态 | 进行中 |

## 2. 已定边界

| # | 决定 | 理由 |
|---|---|---|
| B1 | 交付**可演进为 T09 底座的 Go 小接口 + 固定实现 + 测试**，不是一次性脚本 | 任务池第 13 行、范围文档第 15 行：「可维护的验证代码直接演进，避免先做后扔」 |
| B2 | **不碰 HTTP / 路由 / 前端** | 契约里 `retrieveSources` 标为 optional 且无任何场景驱动；仓库 service 层已完整 |
| B3 | 定位判据放**仓库层**：`StartAt`/`EndAt` 字符区间，结果映射进契约 `locator`/`quoted_text_hash` | 见 ADR-0001；契约的 hash 只能证完整性，不能证出处 |
| B4 | 无模型 → 标 `not_run`，**继续跑完其余断言** | 四条验收里只有一条需要模型，红成一片会掩盖另外三条的结果 |
| B5 | **不落库、不碰迁移、不依赖 MySQL** | 四条验收无一条需要持久化，故不必等 T01 |

## 3. 设计

### 3.1 包位置

新建 `internal/evidence/`，沿用仓库已有小领域包的做法（`internal/ipclass`、`internal/embedpolicy`、`internal/storageallowlist`）。T09 落地时接管。

### 3.2 接口形状

契约 `02-接口与Mock约定.md §2` 只给了两个方法签名（`AssetGateway.ResolveAllowed(projectID, actor, assetIDs)`、`SourcePolicy.Validate(projectID, actor, sourceRefs)`），**没有规定返回值如何承载被拒项**。

这是 T03 必须自己定义的部分，也是本任务最该交给 T09 的东西：

```go
// ResolveResult 一次性说清"请求了什么、允许了什么、拒绝了什么"。
// 三个字段缺一不可：只返回 Allowed 就是契约里那句被禁止的"静默当全部成功"。
type ResolveResult struct {
    Requested []string      // 回显请求集合，供消费者逐项比对
    Allowed   []Asset       // 绑定且就绪且当前可访问
    Denied    []DeniedAsset // 含原因：未授权 / 未就绪 / 不存在
}

type AssetGateway interface {
    ResolveAllowed(ctx context.Context, projectID string, actor Actor, assetIDs []string) (*ResolveResult, error)
}
```

`Source` 按契约字段构造，另挂一个**不序列化**的仓库锚点供本层校验：

```go
type Source struct {
    ID, ProjectID, AssetID, AssetRevision string
    Locator, QuotedText, QuotedTextHash  string
    Status                                SourceStatus // available | stale | unavailable
    anchor                                Anchor       // KnowledgeID/ChunkID/ChunkIndex/StartAt/EndAt/ContentRevision
}
```

### 3.3 来源定位与失效检测

术语表 §2 已定义「来源锚点 `SourceAnchor`：能回到材料中具体页、段、表、图或**字符区间**的位置描述；应支持**重定位和失效检测**。**不应混同为 WeKnora 的裸 `chunk_id`**」。

仓库的 `types.SearchResult`（`internal/types/search.go:151-227`）已提供 `StartAt`/`EndAt`（原文字符区间）、`ChunkIndex`、`KnowledgeID`、`ID`，并有失效钩子 `ContentRevision` / `ContentRewritten`。**但这两个字段是 `json:"-"`，序列化即丢失**——所以失效检测必须在同一进程内完成，不能靠回读持久化 JSON 补判。

**偏移的单位是 rune。** 依据：`chat_pipeline/merge_overlap.go:98` 已有的不变量 `runeLen(Content) == EndAt-StartAt`，以及 `chunk.go:631` 用 `len(baseRunes)` 作上界。按字节切会静默切错中文，强档断言正是用来抓这个错的。

**「原文」从哪来（2026-09-23 查证）。** 全量解析产物**没有落库**：`types.Knowledge` 只有 `FilePath`/`Metadata`/`ParseStatus`，无正文字段；`Chunk.SourceContent` 是逐块产物，不是整篇。现有代码把这对坐标当**相对量**而非绝对指针——`merge_overlap.go:41` 用 `EndAt-StartAt` 算重叠长度，`chunk.go:630` 用 `child.StartAt-parent.StartAt` 抵到父块，`knowledge_search.go:1224` 拿它当位置先验，`merge.go:270` 的注释称其为「editable StartAt/EndAt coordinates」（merge pipeline 自己会改写 `EndAt`）。

所以「能指回原文」分两档，**不可混为一谈**：

| 档 | 判据 | 适用范围 |
|---|---|---|
| **强（绝对）** | `原文[StartAt:EndAt] == QuotedText` | **仅当原始资料是可直读的纯文本**（本轮合成资料即如此）。只有这档能检出 rune/byte 混淆 |
| **弱（相对自洽）** | `runeLen(Content) == EndAt-StartAt` ∧ `ContentRevision == 0` ∧ `!ContentRewritten` | 真实资料上唯一能诚实声明的一档 |

**对 PDF/DOCX 等需重解析的格式，本轮只给弱档。** 提升到强档需要一个能拿到原文的落点（存解析产物，或按 `FilePath` 重解析）——**这是 T09 的缺口，见 §6.5**。

因此 `Status` 的取值规则是：

| 条件 | `Status` |
|---|---|
| `ContentRewritten == true`，或 `ContentRevision` 与落库值不一致 | `stale` |
| `StartAt`/`EndAt` 无法从原文取回 `QuotedText` | `unavailable` |
| 上述都不成立 | `available` |

**`stale` 不是失败，是结论**：它表示「这条引用曾经可用，但坐标已不可信」，下游据此拒绝把它当作当前证据。

### 3.4 报告与运行模式

契约 §9 要求运行模式显式标 `mock` / `real_api_fake_model` / `real`。T03 的报告同时记录两个维度：

- 整体 `mode`：本次实际使用的最高保真度；
- 逐条 `status`：`passed` / `failed` / **`not_run`**（附原因）。

`not_run` 不得被算作 `passed`，也不得让整体 `mode` 升级。**没有真实模型输出时，`mode` 永远不是 `real`。**

## 4. TDD 切片

每片一红一绿，先写断言再写实现。

| 片 | 断言（红） | 覆盖验收 |
|---|---|---|
| S1 | 请求 3 个资料、1 个未授权 → `Allowed` 2 项、`Denied` 1 项、`Requested` 3 项；**不得**只返回 2 项就了事 | 部分授权不能静默当全部成功 |
| S2 | 请求空集合 → `Allowed` 为空，且**检索引擎调用次数为 0** | 空范围不扩大为全库 |
| S3 | 未就绪资料 → 进 `Denied`（原因 `not_ready`），**不得**用空解析结果充当证据 | 契约 F03 |
| S4 | 正常命中 → `原文[StartAt:EndAt] == QuotedText`，且 `sha256(utf8(QuotedText)) == QuotedTextHash` | 能指回原文 |
| S5 | `ContentRewritten == true` 的命中 → `Status == stale`，**不得**报 `available` | 失效检测（术语表 `SourceAnchor` 要求） |
| S6 | 模型不可用 → 该条 `not_run`、其余条照常执行、`mode != real` | 无模型权限时如实标未完成 |

可复用的现成替身：`fakeRetrieveEngineService`（`internal/application/service/knowledgebase_search_fanout_test.go:381`，自带 `retrieveCalls` 计数与 `cannedErr`）用于 S2；`stubModelService` / `stubChatModel`（`internal/application/service/memory/stubs_test.go:120,227`，带 `failNext`）用于 S6。仓库的替身分散在各包、不可跨包复用，T03 需要新替身时按既有做法就地复制。

运行方式：`make test`（= `go test -v ./...`），无需外部依赖。

### 一次真实模型输出（S7，环境可用时）

现成脚手架：`//go:build acceptance_e2e` 的 `TestRAGFullLoop`（`cli/acceptance/e2e/e2e_test.go:36`），环境变量 `WEKNORA_E2E_HOST` / `WEKNORA_E2E_TOKEN` / `WEKNORA_E2E_CHAT_MODEL`。

**当前状态：`not_run`。** 查证于 2026-09-23：上述环境变量均未设置，Docker daemon 未运行。

## 5. 失败记录格式

每条失败/未运行记录至少含：场景 ID、期望、实际、**运行模式**、可复现命令。模型类失败另记返回的 `error.code`（契约 `Error` schema）与是否 `retryable`。

现有代码在至少四处**静默降级**（向量化未配置 → `knowledgebase_search.go:206-208` 只留关键词；chat pipeline embedding 失败 → `chat_pipeline/search.go:417-440` 部分降级；rerank 调用失败 → `rerank.go:130-144` 只打 warn；Agent 路径 rerank 却是硬错 → `session_agent_qa.go:127-137`）。**T03 只如实记录，不修改这些行为**——它们不属于本任务范围，改动会扩散到 T04/T05 之外的模块，违反「无关重构不夹入功能 PR」。

## 6. 未决与风险

### 6.1 评审人空缺

`05-团队分工与代码边界.md §5` 要求契约变化由**实际消费者**评审，T03 的消费者是 T09。**T09 目前无人领取，评审人空缺**——该状态会写进 PR 描述，不因缺人而降级为「无需评审」。本轮 T03 自己定义的返回值形状（§3.2）应当由 T09 领取者第一个过目。

### 6.2 契约缺「部分授权」场景

`contracts/scenarios.json` 中：F02 只测空集合（400），F07 只测全撤权（403），**没有任何「请求 N 个、授权 M 个」的场景**；`retrieveSources` 响应也没有 allowed / skipped / denied 子集字段。这恰是 T03 验收第 3 条，也是本轮唯一既无字段、又无场景、又无断言的验收项。是否补写该场景见 §8。

### 6.3 引用标记格式与仓库现有协议冲突

契约 §5 规定正文引用统一为 `[[source:来源ID]]`；而仓库已有完整引用协议：模型侧私有句柄 `<ref id="cN"/>` ↔ 前端公开 `<kb doc chunk_id kb_id />`（`internal/modelcontext/citations.go`），另有 SSE `references` 事件（`internal/event/event.go:57`）。且 `[[source:` 全仓零命中，前端 `citationMarkdown.ts:186` 把 `[[...]]` 当 **wiki 内链**渲染。

**该冲突超出 T03 范围**（属 T11/T12/T13 的正文与确认链），但会在 T03 产出 `source_id` 之后立刻撞上。建议另立 ADR 与任务，不在本 PR 内悄悄改格式。

### 6.4 未测风险

- 真实模型回路未跑通，模型质量与失败语义无真实证据（§4 S7）。
- 合成资料尚未选定，S4 的「原文取回」依赖它稳定不变。

### 6.5 真实资料上只有弱档定位（登记给 T09）

§3.3 的两档里，强档依赖「原始资料是可直读的纯文本」。真实资料多为 PDF/DOCX，而全量解析产物没有落库、`Knowledge.FilePath` 指向原始文件而非解析结果。所以 T09 之后要在真实资料上做「指回原文」，必须先决定原文的落点：存解析产物，还是按 `FilePath` 重解析。

T03 不解决它，只标注出来——本任务对真实资料能诚实声明的上限是**弱档**。

## 7. 交接证据

待填。PR 描述按 `05-团队分工与代码边界.md §5` 写：用户结果、接口变化、验证证据、仍为 Mock 的部分、回退办法。

## 8. 待定

**是否补写「部分授权」契约场景**（§6.2）：补则触及 `contracts/scenarios.json` 与 `contracts/openapi.json`，属契约变更，需 T09 评审（当前空缺）。
