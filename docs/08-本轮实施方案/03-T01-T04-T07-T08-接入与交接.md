# 工作区基础切片：T01 / T04 / T07 / T08

本切片接入现有 WeKnora 服务，不新增账号、服务进程或前端请求层。它提供两章**演示模板**下的项目创建、研究条件保存、立项、章节手动编辑与 `GenerationContext`。该模板不是正式申报模板；资料绑定、带来源的正文、生成、确认和导出分别等待 T02 / T09–T14。

## 接在哪里

- 后端：`internal/lingdoc/workspacecore` 是只依赖 GORM 的领域服务；`internal/lingdoc/workspace` 是 Gin 薄适配；`internal/container` 注入实例；`internal/router` 在既有认证路由组内挂 `/api/v1/lingdoc/projects`。
- 存储：PostgreSQL `migrations/versioned/000097_lingdoc_workspace.*.sql`，SQLite `migrations/sqlite/000018_lingdoc_workspace.*.sql`。两者沿用仓库的迁移入口，启动时不运行 `AutoMigrate`。
- 前端：`frontend/src/api/lingdoc/workspace.ts` 沿用当前 Axios 请求封装；`/platform/lingdoc` 挂载项目、研究条件和章节编辑切片。

## 本机复核与运行

在仓库根目录执行 `go test ./internal/lingdoc/workspacecore -count=1`，无 C 编译器的 Windows PowerShell 先设置 `$env:CGO_ENABLED='0'`；在 `frontend` 执行 `npm ci`、`npm run type-check`、`npm run build-only`。测试使用隔离的磁盘 SQLite，并执行同一份生产 SQLite 迁移 SQL；覆盖重启读回、同版本双写、响应丢失重试、非成员隐藏、成员移除后的重放拒绝。独立启动和登录沿用仓库现有说明；登录后打开 `/platform/lingdoc`。开发时可选仓库已有的 SQLite/Lite 模式，部署 PostgreSQL 时执行对应版本迁移。密钥仍按仓库原方式放在环境配置，不写入项目资料表或仓库。

**此前阶段记录（已由下方 2026-09-23 后续实测更新）**：无 C 编译器的 Windows 宿主机可能在仓库已有的 `pg_query` 依赖处无法通过全服务 Go 构建；本机一次 Docker Desktop 启动也曾因 WSL 服务异常失败。当时只完成领域服务、SQLite 迁移及前端类型/产物构建，尚未完成整服务启动、PostgreSQL 迁移和浏览器登录联调。以上只代表此前状态，不应作为当前验收结论。T01 的“第二人能启动”验收仍待补做。

**2026-09-23 复核实况（DSH·dsh改造师；上文保留为历史证据，勿据此判本机不可构建）**：本机**存在可用 C 编译器**——随 MATLAB 安装的 mingw-w64，`C:\ProgramData\MATLAB\SupportPackages\R2024a\3P.instrset\mingw_w64.instrset\bin\gcc.exe`（`x86_64-posix-seh-rev0`，8.1.0）；仅加进程 PATH/`CC`，实测 `CGO_ENABLED=1 go build ./internal/utils`（`pg_query_go` 在本仓的唯一引入处）**退出码 0**。故"Windows 宿主机无 C 编译器 ⇒ 整个服务构建在 `pg_query` 处失败"**在本机不成立**。另：沙箱内 `proxy.golang.org`／`registry.npmjs.org` 等端点不可达，但 **`GOPROXY=https://goproxy.cn` 可达**，依赖可补齐；WSL／Docker 本轮未再触碰，本路线不需要它们。该单包结果不替代整服务验收；Lite 构建、服务及浏览器联调状态以紧随其后的后续实测记录为准。

**后续完整 Lite 验收（2026-09-23；本机证据，非仓内交付）**：原始记录 `D:/Mon3tr/projects/lingdoc-dachuang/acceptance/USER-ACCEPTANCE.md` 位于独立仓库 `repo/` 之外，且被现场忽略规则排除；记录、构建日志、验证脚本、截图、账号文件及 CGO 辅助文件均不随本 PR 提供，GitHub PR 读者和新克隆者无法访问。它们不是仓内交付物。仓内保留本机验收摘要与最小复核命令：在仓库根目录运行 `go test ./internal/lingdoc/workspacecore -count=1` 和 `go vet ./internal/lingdoc/workspacecore`；在 `frontend` 运行 `npm ci`、`npm run type-check`、`npm run build-only`。完整 Lite 构建命令为 `go build -p 2 -tags sqlite_fts5 -o ..\acceptance\WeKnora-lite.exe ./cmd/server`，但只在本机官方 UCRT GCC 16.2 工具链、`CGO_ENABLED=1`、`CC`/`CXX`、包含仓外 `acceptance/cgo-include` 的 `CGO_CFLAGS`、指向该工具链库目录的 `CGO_LDFLAGS` 均已配置时可复现；所需工具链和辅助文件不在 PR 中。记录结果为：DuckDB 探针输出 `PROBE_OK duckdb=v1.5.2 rows=2`；Lite 二进制构建成功（265,051,136 B）并实际启动；真实 API 注册/登录、建项目、保存条件、立项、章节版本、同键重试和旧版本冲突通过；服务重启后登录与读回通过；Edge 登录、进入灵档页并打开示例项目和章节通过。API/浏览器脚本使用本机账号且未分发，截图和原始日志也留在仓外。本机结果不代表干净克隆可独立复现整套 Lite 验收。

**仍待验证/验收**：PostgreSQL 迁移尚未验证；浏览器手动创建并保存项目、双标签冲突提示未由自动浏览器覆盖；T01“第二人能启动”与 gong 最终验收仍待完成。交接回执记录 gong 已初步浏览且未见明显问题，但这不代表最终验收。WSL／Docker 在 Lite 验收后未重测；当前 SQLite/Lite 路径不依赖它们。

## 向后续任务交什么

| 消费方 | 入口 | 约束 |
| --- | --- | --- |
| T02 | `TemplateReader.Get(id, version)` | 当前 `ContractDemoTemplate` 仅供两章演示；替换时保持项目锁定的模板版本。 |
| T09 | `Service.Authorize(ctx, actor, projectID, capability)` | 检查活跃租户成员及项目成员；来源授权由 T09 自己检查，禁止把两者递归合并。 |
| T10 | `Service.GenerationContext(ctx, actor, projectID, chapterID)` | 一次一致读返回条件、模板引用、当前章与版本；生成任务应把这些版本记入 basis。 |
| T11/T12 | `Service.SaveChapter(...)` 及 `lingdoc_chapter_versions` | 章版本不可变，手动编辑继承全部待核事项，按预期章版本与条件版本拒绝冲突；候选采纳和确认需另做服务操作。 |

`Idempotency-Key` 按操作者、操作和目标记录请求体 hash，领域写入与响应同事务保存；历史重放先检查**当前**项目权限，再返回原结果并提示刷新。前端仅能编辑无来源的章节，后端也拒绝写入或改写带来源章节；T09 接入 `SourcePolicy` 后才放开，不能仅去掉前端禁用态。`access-status` 当前先核对项目成员资格；来源授权尚未接入前，`content_access` 返回 `unknown`，不能用它放行来源内容。T09 接入后再返回来源当前状态。
